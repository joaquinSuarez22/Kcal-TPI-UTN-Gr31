<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout reutilizable para las tarjetas de categorías de comida en el Dashboard.
    Representa una comida específica (Desayuno, Almuerzo, etc.) mostrando:
    - Un icono/emoji representativo.
    - El nombre de la categoría.
    - El progreso calórico (consumido vs recomendado).
    - Un botón circular (+) para abrir la pantalla de registro.
-->
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="12dp"
    android:layout_marginHorizontal="4dp"
    app:cardBackgroundColor="@android:color/white"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Icono visual de la comida -->
            <TextView
                android:id="@+id/imgComidaIcono"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:text="🥐"
                android:textSize="24sp" />

            <!-- Etiqueta de la categoría -->
            <TextView
                android:id="@+id/txtComidaNombre"
                style="@style/TextAppearance.Kcal.SubHeading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerVertical="true"
                android:layout_marginStart="12dp"
                android:layout_toEndOf="@id/imgComidaIcono"
                android:text="Nombre Comida"
                android:textSize="16sp" />

            <!-- Botón de acción para agregar alimentos -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAgregarComida"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:insetLeft="0dp"
                android:insetTop="0dp"
                android:insetRight="0dp"
                android:insetBottom="0dp"
                android:padding="0dp"
                app:backgroundTint="@color/primary"
                app:cornerRadius="22dp"
                app:elevation="0dp"
                app:icon="@android:drawable/ic_input_add"
                app:iconGravity="textStart"
                app:iconPadding="0dp"
                app:iconSize="24dp"
                app:iconTint="@android:color/white" />

        </RelativeLayout>

        <!-- Información de calorías consumidas en esta categoría -->
        <TextView
            android:id="@+id/txtComidaCalorias"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="0 / 0 kcal"
            android:textColor="#777"
            android:textSize="14sp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>