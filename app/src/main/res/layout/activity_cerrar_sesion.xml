<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout para la pantalla de Confirmación de Cierre de Sesión.
    Presenta una interfaz centrada con un icono de advertencia/salida,
    un mensaje explicativo y botones de acción para confirmar o cancelar la operación.
    Incluye la barra de navegación flotante para mantener la consistencia visual.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_light"
    tools:context=".activities.CerrarSesionActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- Encabezado con título de sección -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp">

            <ImageView
                android:id="@+id/btnBack"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:contentDescription="Volver"
                android:src="@android:drawable/ic_menu_revert"
                app:tint="#131f1a" />

            <TextView
                android:id="@+id/tvHeaderTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:text="Cuenta"
                android:textColor="#131f1a"
                android:textSize="18sp"
                android:textStyle="bold" />
        </RelativeLayout>

        <!-- Contenido central de confirmación -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="24dp"
            android:paddingBottom="100dp"
            android:clipToPadding="false">

            <!-- Contenedor circular para el icono de salida -->
            <FrameLayout
                android:layout_width="96dp"
                android:layout_height="96dp"
                android:layout_marginBottom="32dp"
                android:background="@drawable/circulo_fondo">

                <ImageView
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_gravity="center"
                    android:src="@android:drawable/ic_lock_power_off"
                    app:tint="#1c7d55"
                    android:contentDescription="Icono de cierre de sesión" />
            </FrameLayout>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:gravity="center"
                android:text="¿Querés cerrar sesión?"
                android:textColor="#131f1a"
                android:textSize="24sp"
                android:textStyle="bold" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="48dp"
                android:gravity="center"
                android:maxWidth="280dp"
                android:text="Tendrás que volver a ingresar tus datos para acceder a tu seguimiento nutricional."
                android:textColor="#475569"
                android:textSize="16sp" />

            <!-- Botón principal: Confirma la salida -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnConfirmarCerrarSesion"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginBottom="16dp"
                android:backgroundTint="#1c7d55"
                android:text="Cerrar sesión"
                android:textAllCaps="false"
                android:textColor="#ffffff"
                android:textSize="18sp"
                android:textStyle="bold"
                app:cornerRadius="12dp" />

            <!-- Botón secundario: Cancela y vuelve atrás -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnCancelar"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:text="Cancelar"
                android:textAllCaps="false"
                android:textColor="#1c7d55"
                android:textSize="18sp"
                android:textStyle="bold"
                app:cornerRadius="12dp"
                app:strokeColor="#1c7d55"
                app:strokeWidth="2dp" />

        </LinearLayout>
    </LinearLayout>

    <!-- Inclusión de la barra de navegación flotante -->
    <include layout="@layout/floating_bottom_nav" />

</FrameLayout>